data/
local/
.claude/
# Populated only inside the container image (Dockerfile BUNDLE_MODEL).
/models/
tests/wav/.tts_cache/
__pycache__/
*.pyc
*.fst
*.fst.tokens
.pytest_cache/
lib/build/
lib/*.egg-info/

# Dev launcher: holds a long-lived Home Assistant token inline. Ignored so a
# `git add -A` cannot sweep it into a commit.
run-demo.sh
