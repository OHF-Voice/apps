# The recognition library is vendored in lib/ and built from source here, so the
# add-on has no external git dependency and builds from this checkout alone.
# It pulls its own deps (numpy, onnxruntime, sentencepiece, soundfile, scipy,
# soxr, icu-rbnf); the ones listed below are those the add-on itself imports.
./lib

flask
aiohttp
pyyaml
numpy
soundfile
hassil
home-assistant-intents>=2026.8.25
wyoming[zeroconf]

# Not listed: librosa. The log-mel front end used it for exactly three calls --
# filters.mel, stft and resample -- and it hard-depends on numba and
# scikit-learn, hence llvmlite, whose single shared object was 170 MB, about a
# fifth of the image. lib/speech_to_phrase/_dsp.py does the first two with numpy
# and scipy.fft, and audio.resample calls soxr (which is what librosa delegated
# to anyway). tests/test_features_parity.py holds the two to being
# bit-identical.
#
# Also not listed: scipy. It is a dependency of the vendored library rather than
# of the add-on, since _dsp.py uses scipy.fft -- that is what makes the parity
# exact, where numpy.fft differs in the last bit. tools/audio_test.py imports
# scipy.signal directly and gets it the same way.
